<?php

namespace App\Enums;

enum BaseStatusEnum
{
    // Active = 1,
    // Inactive = 0,
    // Deleted = -1,
    // Archived = -2,
    // Pending = 0,
    // Approved = 1,
    // Rejected = -1,
    // Draft = 0,
    // Published = 1,
    // Unpublished = 0,
    // Suspended = -1,
    // Unsuspended = 1,
    // Locked = -1,
    // Unlocked = 1,
    // Frozen = -1,
    // Unfrozen = 1,
    // Expired = -1,
    // NotExpired = 1,
    // Banned = -1,
    // Unbanned = 1,
    // Hidden = -1,
    // Visible = 1,
    // HiddenByAdmin = -1,
    // VisibleByAdmin = 1,
    // PendingApproval = 0,
    // ApprovedByAdmin = 1,
    // RejectedByAdmin = -1,
    // PendingPayment = 0,
}
